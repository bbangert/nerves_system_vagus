# /etc/nsswitch.conf — vagus-runtime-spike, plan task 1.7
#
# balena-engine (and everything it execs) is a cgo binary doing libc NSS
# lookups for its own operation (e.g. resolving the `balena-engine` system
# user created at build time by BALENA_ENGINE_USERS) and for containers'
# default resolver behavior. Confirmed by reading
# nerves_system_br/board/nerves-common/skeleton/ directly: no
# /etc/nsswitch.conf exists anywhere in that tree or in the rpi0_2
# rootfs_overlay — this file is a genuine gap, not something already
# provided elsewhere (unlike hosts/resolv.conf/passwd/group — see notes.md).

passwd:   files
group:    files
shadow:   files
hosts:    files dns
